# Frontend runtime configuration (Create React App requires REACT_APP_ prefix)
# Copy to .env (or set in your deployment environment) and restart `npm start`.

REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
